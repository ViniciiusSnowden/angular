import { ConsoantesPipe } from './consoantes.pipe';

describe('ConsoantesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsoantesPipe();
    expect(pipe).toBeTruthy();
  });
});
